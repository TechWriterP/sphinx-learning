What is Sphinx?
===============

Sphinx is a documentation generator that converts source files into output such as HTML.

Key ideas
---------

- Source files hold the content.
- ``toctree`` defines the navigation structure.
- ``conf.py`` controls site-wide settings.
- The build command generates the final output.

.. _why-it-matters:

Why it matters
--------------